# features/ai/data/

Reservada para os repositórios/data sources que vão conversar com a API
de IA que for escolhida (ex: `AiRepository`, `OpenAiDataSource` ou
equivalente) — nada disso existe ainda.
